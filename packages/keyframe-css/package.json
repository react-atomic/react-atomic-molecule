{
  "private": true,
  "description": "keyframe lib",
  "repository": "react-atomic/react-atomic-molecule",
  "keywords": ["reactjs"],
  "author": "Hill <hill@kimo.com>",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.x"
  },
  "scripts": {
    "clean": "find ./build/* \\( ! -path \"*node_modules*\" \\) | xargs rm -rf",
    "copy": "cp package.build.json ./build/package.json && cp yarn.build.lock ./build/yarn.lock  && cp README.md ./build/",
    "build:cjs": "BABEL_ENV=cjs babel src -d build --root-mode upward",
    "build:es": "BABEL_ENV=es babel src/index.js -o build/index.es.js --root-mode upward",
    "build:dev": "npm run build:cjs && npm run build:es && npm run copy && cd build && yarn && cp yarn.lock ../yarn.build.lock",
    "build": "npm run clean && npm run build:dev",
    "prepublishOnly": "echo should not publish && exit 1;"
  }
}
