{
  "version": "0.5.1",
  "name": "ricon",
  "repository": {
    "type": "git",
    "url": "https://github.com/react-atomic/react-atomic-molecule",
    "directory": "packages/ricon"
  },
  "homepage": "https://github.com/react-atomic/react-atomic-molecule/tree/main/packages/ricon",
  "description": "Atomic Icon Sets",
  "keywords": ["reactjs"],
  "author": "Hill <hill@kimo.com>",
  "license": "MIT",
  "dependencies": {
    "keyframe-css": "*",
    "react-atomic-molecule": "*",
    "reshow-runtime": "*"
  },
  "devDependencies": {
    "@babel/cli": "^7.x",
    "reshow-unit": "*"
  },
  "exports": {
    "./*": {
      "require": "./build/cjs/ui/*",
      "import": "./build/es/ui/*"
    }
  },
  "scripts": {
    "clean": "find ./build ./types -name '*.*' | xargs rm -rf",
    "build:cjs": "BABEL_ENV=cjs babel ui -d build/cjs/ui --ignore /**/__tests__ --root-mode upward",
    "build:es": "BABEL_ENV=es babel ui -d build/es/ui --out-file-extension .mjs --root-mode upward",
    "build:type": "npx -p typescript tsc ui/*.jsx --jsx react-jsx --declaration --allowJs --emitDeclarationOnly --skipLibCheck --declarationDir types",
    "build": "npm run clean && npm run build:cjs && npm run build:es && npm run build:type",
    "mochaFor": "STRICT_MODE=on mocha -r global-jsdom/register",
    "mocha": "npm run mochaFor -- 'build/es/**/__tests__/*.mjs'",
    "test": "npm run build && npm run mocha",
    "prepublishOnly": "npm run test"
  },
  "typesVersions": {
    "*": {
      "*": ["types/*"]
    }
  },
  "files": ["types", "build", "package.json", "README.md"]
}
