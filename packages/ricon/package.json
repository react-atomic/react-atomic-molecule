{
  "description": "Atomic Icon Sets",
  "repository": "react-atomic/react-atomic-molecule",
  "keywords": [
    "reactjs"
  ],
  "author": "Hill <hill@kimo.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-core": "^6.7.5",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-plugin-transform-react-constant-elements": "^6.9.1",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "react": "^15.0.1"
  },
  "scripts": {
    "copy": "rm -rf build && BABEL_ENV=build babel ui -d build && cp package.json.build ./build/package.json && cp README.md ./build/",
    "build": "npm run copy && cd build && yarn install",
    "prepublish": "npm run build"
  }
}
